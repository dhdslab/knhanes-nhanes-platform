# Core (required) -- TF-IDF search works with just these, no external services.
python-docx>=1.1
scikit-learn>=1.3
scipy>=1.10
numpy>=1.24
pandas>=2.0
streamlit>=1.30
requests>=2.31
# Optional: semantic search + written answers via a local Ollama server
#   install Ollama from https://ollama.com, then:  ollama pull bge-m3 && ollama pull llama3.2
