# Python 3.12.13, the environment used in the study
# Core scientific stack -------------------------------------------------------
numpy==2.4.6
pandas==3.0.3
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
pyreadstat==1.3.5          # reads KNHANES .sas7bdat and NHANES .XPT
# Report generation -----------------------------------------------------------
python-docx==1.2.0         # Word (.docx) output
docxcompose==2.2.0         # merges per-report .docx into the Supplementary Data bundles
openpyxl==3.1.5            # optional spreadsheet export
matplotlib==3.11.0         # ROC/PR, calibration, trend, SHAP/PDP figures
# Machine learning ------------------------------------------------------------
shap==0.52.0
xgboost==3.3.0
# Agents, app, tests ----------------------------------------------------------
requests==2.34.2           # Ollama HTTP client (agents.py, rag/)
streamlit==1.59.0          # web UI (factory_app.py, rag/app.py)
pytest==9.1.1
#
# Optional: the last two members of the 16-model registry (ml_report.py). They were not
# installed in the study environment and are not needed for the released corpus.
#   lightgbm>=4.5
#   catboost>=1.2
#
# R (>= 4.6) is required for all survey statistics, with the packages
#   survey, MASS, jsonlite, survival   (splines ships with R)
# Install them with:  Rscript install_r_packages.R
#
# Ollama with llama3.3:70b is optional; without it the agents fall back to deterministic
# rules and templates. See LOCAL_MODEL.md.
